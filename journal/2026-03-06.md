# 2026-03-06 - Praca nad kodem programu todo_cli

## Cel

- na podstawie diagramów utworzenie plików stanowiących podstawe programu
- słowniki w pythonie

## Komendy

### Tworzenie słownika

```python
sl = {
  "imie": "Anna",
  "nazwisko": "Nowak",
  "wiek": 20
}
```

### Wyświetlanie słownika i wartości słownika

```python
print(sl)

print(sl["imie"])

x = sl.get("imie")
print(X)
```

### Dodawanie wartości

```python
  sl["miasto"] = "Warszawa"
  sl.update({"kraj": "Polska"})
```

### Usuwanie wartości

```python
sl.pop("miasto")
del sl["kraj"]
```

### Pętle

```python
for x in sl:
  print(x) #wyświetlanie kluczy

for x in sl:
  print(sl[x])  #wyświetlanie wartości

for x in sl.keys(){
  print(x)  #wyświetlanie kluczy
}

for x in sl.values(){
  print(x)  #wyświetlanie wartości
}

for x, y in sl.items():
  print(x, y)  #wyświetlanie kluczy i wartości
```
